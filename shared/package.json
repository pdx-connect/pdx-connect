{
  "name": "shared",
  "version": "0.0.1",
  "description": "The shared sub-project.",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "clean": "shx rm -rf ./dist ./test/**/*.js ./test/**/*.js.map ./test/**/*.d.ts"
  }
}
